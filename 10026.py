draw_size = int(input())
draw_info = [list(map(str, input())) for _ in range(draw_size)]
print(draw_info)

chk_adnormal = [[False for _ in range(draw_size)] for _ in range(draw_size)]
answ

for col in range(draw_size):
    for row in range(draw_size):
        if chk_adnormal[col][row] == 1:


chk_normal = [[False for _ in range(draw_size)] for _ in range(draw_size)]

